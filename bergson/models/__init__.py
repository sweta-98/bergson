"""Model-architecture integrations that ship with bergson.

These are self-contained model wrappers that pair with bergson's
training/attribution pipeline but aren't part of core bergson — they
live here so users can point a config at them without vendoring code
into their own project.
"""
